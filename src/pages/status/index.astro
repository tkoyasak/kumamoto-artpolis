---
import StatusTable from "../../components/StatusTable.tsx";
import Base from "../../layouts/Base.astro";
import { getStatusRows } from "../../lib/status.ts";

const rows = await getStatusRows();
---

<Base title="Status">
  <StatusTable client:load slot="status" rows={rows} />
</Base>

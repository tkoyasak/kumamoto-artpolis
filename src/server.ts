// Cloudflare Worker entry (the `main` in wrangler.json).
// Re-exports TanStack Start's default server handler (a default export with `fetch`).
export { default } from "@tanstack/react-start/server-entry";
